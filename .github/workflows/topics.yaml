name: "Deploy topic"
on:
  push:
    paths:
      - nais/topics/**
      - .github/workflows/topics.yaml
    branches:
      - main
  pull_request:
    branches:
      - main
    types: [ opened, synchronize, reopened, ready_for_review ]
  merge_group:
    types: [ checks_requested ]
  workflow_dispatch:
concurrency:
  group: deploy-topics-${{ github.ref }}
  cancel-in-progress: true
env:
  TOPIC_DEV_FILES: nais/topics/syfo-narmestelder-leesah-dev.yaml
  TOPIC_PROD_FILES: nais/topics/syfo-narmestelder-leesah-prod.yaml
jobs:
  deploy-topic-to-dev:
    name: Deploy topic to dev
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - name: Deploy topics to dev
        uses: nais/deploy/actions/deploy@v2 # internal Nav action
        env:
          VAR: kafka-pool=nav-dev
          CLUSTER: dev-gcp
          RESOURCE: ${{ env.TOPIC_DEV_FILES }}

  deploy-topic-to-prod:
    if: github.ref == 'refs/heads/main'
    name: Deploy topic to prod
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - name: Deploy topics to prod
        uses: nais/deploy/actions/deploy@v2 # internal Nav action
        env:
          VAR: kafka-pool=nav-prod
          CLUSTER: prod-gcp
          RESOURCE: ${{ env.TOPIC_PROD_FILES }}
