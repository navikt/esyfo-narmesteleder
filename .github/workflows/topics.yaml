name: "Deploy topic"
on:
  push:
    paths:
      - nais/topics/**
      - .github/workflows/topics.yaml
    branches:
      - main
  pull_request:
    branches:
      - main
    types: [ opened, synchronize, reopened, ready_for_review ]
  merge_group:
    types: [ checks_requested ]
  workflow_dispatch:
env:
  TOPIC_FILES: nais/topics/syfo-narmestelder-leesah-dev.yaml
  TOPIC_DEV_FILES: nais/topics/syfo-narmestelder-leesah-prod.yaml
jobs:
  deploy-topic-to-dev:
    name: Deploy topic to dev
    permissions:
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Deploy topics to dev
        uses: nais/deploy/actions/deploy@v2
        env:
          VAR: kafka-pool=nav-dev
          CLUSTER: dev-gcp
          RESOURCE: ${{ env.TOPIC_DEV_FILES }}

  deploy-topic-to-prod:
    if: github.ref == 'refs/heads/main'
    name: Deploy topic to prod
    permissions:
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Deploy topics to prod
        uses: nais/deploy/actions/deploy@v2
        env:
          VAR: kafka-pool=nav-prod
          CLUSTER: prod-gcp
          RESOURCE: ${{ env.TOPIC_PROD_FILES }}
